

const VERSION = "/api/v1";
const COACH = `${VERSION}/coachs`
const PLAYER = `${VERSION}/players`
const TASK = `${VERSION}/tasks`
const ADMIN = `${VERSION}/admin`




module.exports = {
    ADMIN,
    COACH,
    PLAYER, TASK
}



